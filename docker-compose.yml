services:
  nextjs:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/data/prisma/dev.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data: